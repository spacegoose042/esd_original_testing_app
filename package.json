{
    "name": "esd-testing-app",
    "version": "1.0.0",
    "scripts": {
      "postinstall": "cd server && npm install && cd ../client && npm install",
      "build": "cd client && npm run build && cd ../server && npx prisma generate && npx prisma migrate deploy",
      "start": "cd server && npm start"
    },
    "engines": {
      "node": ">=14.0.0"
    },
    "dependencies": {
      "express": "^4.17.1",
      "@prisma/client": "^5.7.1",
      "prisma": "^5.7.1"
    }
  }