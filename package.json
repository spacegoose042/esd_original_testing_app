{
  "name": "esd-testing-app",
  "version": "1.0.0",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "build": "cd server && rm -rf node_modules && npm install && cd ../client && npm install && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}