{
    "name": "esd-testing-app",
    "version": "1.0.0",
    "scripts": {
      "start": "node server/server.js",
      "build": "cd client && npm install && npm run build && npx prisma generate && npx prisma migrate deploy",
      "install-client": "cd client && npm install",
      "heroku-postbuild": "npm run install-client && npm run build"
    },
    "engines": {
      "node": ">=14.0.0"
    },
    "dependencies": {
      "express": "^4.17.1"
    }
  }