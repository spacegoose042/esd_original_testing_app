{
    "name": "esd-testing-app",
    "version": "1.0.0",
    "scripts": {
      "build": "cd client && npm install && npm run build",
      "postbuild": "cd server && npm install && npx prisma generate && npx prisma migrate deploy",
      "start": "cd server && node server.js"
    },
    "engines": {
      "node": ">=14.0.0"
    },
    "dependencies": {
      "express": "^4.17.1"
    }
  }