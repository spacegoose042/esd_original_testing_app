{
    "name": "esd-testing-app",
    "version": "1.0.0",
    "scripts": {
      "build": "cd server && npm install && cd ../client && npm install && npm run build",
      "start": "cd server && echo 'PWD:' && pwd && echo 'Files:' && ls -la && npm start"
    },
    "engines": {
      "node": ">=14.0.0"
    },
    "dependencies": {
      "express": "^4.17.1"
    }
  }