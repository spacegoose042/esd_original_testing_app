{
  "name": "esd-testing-app",
  "version": "1.0.0",
  "scripts": {
    "force-nullable": "cd server && node scripts/force_email_nullable.js",
    "build": "cd client && npm install && npm run build",
    "start": "npm run build && cd server && npm start",
    "dev": "cd server && npm run dev",
    "add-active-status": "cd server && node scripts/add_active_status.js",
    "add-is-active": "cd server && node scripts/add_is_active_column.js"
  }
}